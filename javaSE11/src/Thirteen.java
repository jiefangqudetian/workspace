public class Thirteen {

	public static void main(String[] args) {

//		int num[] = { 89, 99, 12, 35, 36 };
//		int max = 0;
//		for (int i = 0; i < num.length; i++) {
//			if (max < num[i]) {
//				max = num[i];
//			}
//		}
//		System.out.println(max);

		// 第一题
		/*
		  int A = 0; 
		  boolean flag = true; 
		  yunsuan jisuan = new yunsuan();
		  java.util.Scanner input = new java.util.Scanner(System.in);
		  System.out.println("请输入第一个数字"); 
		  int one = input.nextInt();
		  System.out.println("请输入第二个数字"); 
		  int two = input.nextInt();
		  System.out.println("请输入要进行的运算");
		  System.out.println("两数相加请按+，两数相减请按-，两数相乘请按*，两数相除请按/"); 
		  String temp =input.next(); 
		  if (temp.equals("+")) { 
			  A = jisuan.jia(one, two); 
			  }else if (temp.equals("-")) { 
			  A = jisuan.jian(one, two); 
			  }else if(temp.equals("*"))  {
			  A = jisuan.cheng(one, two); 
			  }else if(temp.equals("/"))  { 
				  if (two == 0) {
					  flag = false;
		              System.out.print("除数不能为0"); } //可用System.exit(0);直接退出
		              else {
		            	  A =jisuan.chu(one, two); } 
				  } 
		  if (flag) { 
					  System.out.println(A); 
					  } */
				  
		 
		// if(two!=0){System.out.println(A);}
		// 第二题
		/*
		 * Computer net = new Computer(); java.util.Scanner input = new
		 * java.util.Scanner(System.in); System.out.println("请输入要播放的音乐"); String
		 * music = input.next(); net.play(music); System.out.println("请输入聊天对象");
		 * String name = input.next(); net.chat(name);
		 */
		// 第三题
		/*
		 * Contacts num = new Contacts(); java.util.Scanner input = new
		 * java.util.Scanner(System.in); System.out.println("请输入姓名"); String
		 * name = input.next(); num.number(name);
		 */

		// 第三题;

		
		  PC3 pc = new PC3(); 
		  pc.startup();
		 
		// int[] nums = new int[10];
		//String[] nums = new String[10];
		// nums[0] = "hello";
		/*
		 * if(nums[0].equals(null)){ System.out.println("略略略"); }else{
		 * System.out.println("无法输出");
		 * 
		 * }
		 */

		// dog gou = new dog();
		//int num = 5 / 0;
		//System.out.println(num);

	}}
